import Section from '~/components/Section'
import { contact } from '~/content/contact'

const Contact = (): JSX.Element => {
  return <Section content={contact} title="Contact" />
}

export default Contact
